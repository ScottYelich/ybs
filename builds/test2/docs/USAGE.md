# Using test2

**Version**: 0.1.0

## Basic Usage

```bash
swift run test2 [options]
```

## Options

*(To be filled in as features are implemented)*

- `--help`: Show help message (not yet implemented)
- `--version`: Show version (not yet implemented)

More options will be added as the implementation progresses.

## Examples

*(To be filled in as features are implemented)*

### Example 1: Basic Chat

```bash
# This will be implemented in future steps
swift run test2
```

## Configuration

Configuration will be via JSON file (to be implemented in step 3).

Planned configuration locations:
- `/etc/ybs/config.json` (system-wide)
- `~/.config/ybs/config.json` (user defaults)
- `~/.ybs.json` (user home)
- `./.ybs.json` (project-specific)
- `--config <path>` (CLI override)

---

**Last updated**: 2026-01-17
