# Session scratchpad (crash recovery) - transient file, never commit
SESSION.md
# Note: SESSION.md.template SHOULD be committed (it's the template)

# Session work files directory - transient files, never commit
scratch/
# Use scratch/ for analysis documents, working files, temporary artifacts
# Clean up after each step or session completes successfully

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Build artifacts
builds/*/build/
builds/*/.build/
builds/*/dist/
builds/*/target/
builds/*/__pycache__/
builds/*/*.egg-info/
builds/*/node_modules/

# Build artifacts in new structure
systems/*/builds/*/build/
systems/*/builds/*/.build/
systems/*/builds/*/dist/
systems/*/builds/*/target/
systems/*/builds/*/__pycache__/
systems/*/builds/*/*.egg-info/
systems/*/builds/*/node_modules/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Temporary files
*.tmp
*.log
.*.tmp
