# Session scratchpad (crash recovery) - transient file, never commit
SESSION.md
# Note: SESSION.md.template SHOULD be committed (it's the template)

# Session work files directory - transient files, never commit
scratch/
# Use scratch/ for analysis documents, working files, temporary artifacts
# Clean up after each step or session completes successfully

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Build artifacts - language/platform-specific outputs
**/builds/*/build/
**/builds/*/.build/
**/builds/*/dist/
**/builds/*/target/
**/builds/*/__pycache__/
**/builds/*/*.egg-info/
**/builds/*/node_modules/
**/builds/*/*.pyc
**/builds/*/.pytest_cache/
**/builds/*/venv/
**/builds/*/env/

# Build workspaces - all build directories are transient (generated from specs/steps)
# Ignore all build workspaces in examples
examples/*/builds/*/

# Ignore all build workspaces in legacy-systems (if any)
legacy-systems/*/builds/*/

# Keep builds/ directories themselves (structure) but ignore contents
!examples/*/builds/
!legacy-systems/*/builds/
!examples/*/builds/.gitkeep
!legacy-systems/*/builds/.gitkeep

# Exception: Keep demo/ builds as reference (if they contain example outputs)
# Uncomment the following if you want to commit demo builds:
# !examples/*/builds/demo/
# !examples/*/builds/demo/**

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Temporary files
*.tmp
*.log
.*.tmp

# Build-specific files that might leak into root
BUILD_CONFIG.json
BUILD_STATUS.md
TEST_REPORT.md
EXECUTION_NOTES.txt
PROJECT_STRUCTURE.txt

# Python
*.pyc
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
*.egg-info/
venv/
env/

# JavaScript/Node
node_modules/
package-lock.json
yarn.lock

# Rust
target/
Cargo.lock

# Go
go.sum

# Swift
.build/
.swiftpm/
*.xcodeproj/
*.xcworkspace/

# Compiled binaries (anywhere)
*.exe
*.out
*.app
*.o
*.so
*.dylib
*.dll
